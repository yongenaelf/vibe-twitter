# Backend API URL (required for production builds)
# Development: Leave empty to use Vite proxy (/api -> localhost:3000)
# Production: Set to full backend URL (e.g., https://myapp-api.chrono-ai.fun)
API_URL=
